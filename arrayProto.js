// as we know array is also a object array is alo stored in a object
let arrays = [1,2,3];
// info shows all the information if thet variable
// all the method of array { pop,slice,shift etc} are stored in the object proto and also the length property is stored in this object
// this object of arrays variable is not shown in the terminal it is visable in the console of chrome brouser
console.info(arrays)